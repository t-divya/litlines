import styled from "styled-components";

const StyledTitle = styled.h1`
  margin: 0;
  text-align: center;
  color: white;
`;

export default function Heading() {
  return <StyledTitle>LitLines</StyledTitle>;
}
